specVersion: "v1beta"
serviceId: "countrymate"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "countrymate-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
